# Copy to .env.local for local dev, and set as a build-time environment
# variable in the Cloudflare Pages project settings for the deployed site.
#
# TODO(user): after your first real backend deploy, set this to its public URL,
# no trailing slash. Examples:
#   Google Cloud Run : https://road-designer-api-xxxxxxxxxx-ew.a.run.app
#   Hugging Face      : https://<user>-<space>.hf.space
VITE_API_BASE_URL=http://localhost:8000
