# Copy to .env.local for local dev, and set as a build-time environment
# variable in the Cloudflare Pages project settings for the deployed site.
#
# TODO(user): after your first real FastAPI deploy on Hugging Face Spaces,
# set this to that Space's public URL (Docker Spaces default to
# https://<user>-<space>.hf.space). No trailing slash.
VITE_API_BASE_URL=http://localhost:8000
